<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.iPlanPro.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- รองรับ Android 12 Splash Screen API -->
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logoapple</item>
        <item name="android:windowSplashScreenIconBackgroundColor">#FFFFFF</item>
        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="31">@null</item>

        <!-- สำรองพื้นหลัง สำหรับ Android ต่ำกว่า 12 -->
        <item name="android:windowBackground">#FFFFFF</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>